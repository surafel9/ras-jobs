const resetPassword = async (req, res, next) => {
	//check if the email is valid
	//check for phone number verification could be done
	//send a code via email -- node mailer with yahoo to be configured
};
